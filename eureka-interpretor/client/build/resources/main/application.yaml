server:
  port: 10001
spring:
  application:
    name: eureka-client
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 2000